document.addEventListener("DOMContentLoaded", () => {
    const todoList = document.querySelector("#todoLista");
    const todoSearch = document.querySelector("#todoSearch");

    function addTask(text) {
        console.log("ebe ebe ebebebe")
    }
    addTask();
});